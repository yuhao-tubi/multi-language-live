# Server Configuration
HOST=localhost
PORT=4000

# Fragment Delivery Settings
FRAGMENT_DATA_INTERVAL=15000
ACK_TIMEOUT_MS=5000
MAX_RETRIES=3
MAX_FRAGMENTS_PER_STREAM=4

# Client Configuration (for test scripts)
STREAM_ID=stream-1

# Assets Path (optional)
# By default, the service always reads from the SOURCE directory:
# {workspace}/apps/mock-media-service/src/assets/audio-fragments
#
# Only set ASSETS_PATH if you need to override this location:
# ASSETS_PATH=/custom/path/to/audio-fragments
